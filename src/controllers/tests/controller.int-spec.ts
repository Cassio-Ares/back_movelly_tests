/**
 * describe
 * 
 *  beforeAll (roda antes de todos os testes) (uma vez antes de tudo limpa meu setup)
 * 
 * beforeEach (roda antes de cada teste)   (limpa a sujeira entes de rodar um novo test)
 * 
 * it
 * 
 *  afterEach(roda depois de cada teste)(limpa a sujeira apos rodar test)
 * 
 * 
 * afterAll (roda depois que todos os testes rodarem)  (desliga meu setup)
 */

describe('', () => {
    it("", ()=>{
         expect(true).toBe(true);
    })
});